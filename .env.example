# Copy this file to .env and fill in real values.
# Public keys only — never commit the Supabase service role key.

# Supabase project URL + anon (public) key for Auth + Realtime.
VITE_SUPABASE_URL="https://rdpfgxlshjxfwzwieker.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# Deployed Express API base URL (the backend prefixes all routes with /api).
VITE_API_URL="https://backend-sfa.vercel.app"

# this is just a comment 
